{% extends 'base.html.twig' %}

{% block body %}
    <h1>Domaineintervention</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ domaineintervention.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ domaineintervention.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('domaineintervention_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('domaineintervention_edit', { 'id': domaineintervention.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
